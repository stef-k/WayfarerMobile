<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WayfarerMobile.Shared.Controls.LoadingOverlay"
             x:Name="thisControl">

    <Border BackgroundColor="#80000000"
            IsVisible="{Binding IsVisible, Source={x:Reference thisControl}}"
            InputTransparent="False"
            ZIndex="100">

        <Border VerticalOptions="Center"
                HorizontalOptions="Center"
                Padding="24"
                StrokeThickness="0"
                BackgroundColor="{StaticResource Surface}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="16" />
            </Border.StrokeShape>

            <VerticalStackLayout Spacing="16"
                                 HorizontalOptions="Center">
                <ActivityIndicator IsRunning="True"
                                   Color="{StaticResource Primary}"
                                   WidthRequest="48"
                                   HeightRequest="48" />

                <Label Text="{Binding Message, Source={x:Reference thisControl}}"
                       Style="{StaticResource Body}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center"
                       IsVisible="{Binding Message, Source={x:Reference thisControl}, Converter={StaticResource IsNotNullConverter}}" />
            </VerticalStackLayout>
        </Border>
    </Border>
</ContentView>
